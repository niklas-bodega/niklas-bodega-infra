apiVersion: v1
kind: Service
metadata:
  name: frontend-service-service
spec:
  type: NodePort
  selector:
    app: frontend-service
  ports:
    - port: 8087
      targetPort: 8087
      nodePort: 30000
